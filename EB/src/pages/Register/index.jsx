import {Form, Alert} from 'react-bootstrap'

const register = ()=>{
    return(
        <div className="register">
            <div className="card">
                <form onSubmit={}>
                    <input type="text" onChange={null}  placeholder='email'/>
                </form>
            </div>
        </div>
    )
}